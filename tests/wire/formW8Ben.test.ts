/**
 * This file was auto-generated by Fern from our API Definition.
 */

import { AboundClient } from "../../src/Client";

const client = new AboundClient({
    environment: process.env.TESTS_BASE_URL || "test",
    token: process.env.TESTS_AUTH || "test",
});

describe("FormW8Ben", () => {
    test("list", async () => {
        const response = await client.formW8Ben.list();
        expect(response).toEqual([]);
    });

    test("create", async () => {
        const response = await client.formW8Ben.create({
            key: "value",
        });
        expect(response).toEqual({ key: "value" });
    });

    test("retrieve", async () => {
        const response = await client.formW8Ben.retrieve("string");
        expect(response).toEqual({ key: "value" });
    });
});
